<!DOCTYPE html>
<html>
  <head>
    <title>Warehouse App - Learning with Campus Code</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <header>
      <h1>Warehouses & Stock</h1>
      <nav>
        <%= link_to 'Suppliers', suppliers_path %>
        <%= link_to 'Product Models', product_models_path %>
        <% if user_signed_in? %>
          <span><%= current_user.email %></span>
          <%= link_to 'Sign out', root_path %>
        <% else %>
          <%= link_to 'Sign in', new_user_session_path %>
        <% end %>
      </nav>
    </header>
    <main>
      <div>
        <p><%= notice %></p>
        <p><%= alert %></p>
      </div>
      <%= yield %>
    </main>
  </body>
</html>
